Ensure that you are in the wally environment, you can run the code with -O optimization with the following command:
make
You can run the code with -O2 optimization with the following command:
make sim2